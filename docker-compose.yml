version: '3.9'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DB_PATH=/data/data.db
    volumes:
      - db-data:/data
    command: ["python", "server.py", "--port", "8000", "--db", "/data/data.db"]
volumes:
  db-data:
